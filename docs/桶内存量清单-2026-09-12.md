# 桶内存量清单与清理记录 · 2026-09-12

桶 `haoqiu-ai-media-1352817304`（ap-shanghai）。用 `haoqiu-init` 维护版扫描得出。

> **已执行（23:05，范围 C · 全清）**：362 个对象 / **1375.96 MB** 全部删除，**零报错**。
> 复核扫描结果：所有前缀归零，仅保留 `deploy/a01-vlm-20260908.zip`（7.54 MB，回滚料）。
> `haoqiu-init` 已部署回原版（用 `{"action":"inventory"}` 触发返回 `confirmation_required` 验证通过）。
> 清理前的成功分析样本已留存本地：`samples/最后一次成功分析-rc_54fb31bc.json`（6 名球员 / 6 个事件）。

## 全量（363 个对象 / 1383.5 MB）

| 前缀 | 数量 | 体积 | 最老 ~ 最新 | 性质 |
|---|---|---|---|---|
| `inputs/` | 18 | **1163.31 MB** | 1.9 天 ~ 今天 | 原始上传视频。**生命周期在管，明后天会自清** |
| `outputs/` | 19 | **206.31 MB** | 17.5 ~ 13.9 天 | **YOLO 标注视频。功能已删 → 永久垃圾** |
| `db/task/` | 46 | 0.59 MB | 17.5 ~ 11.0 天 | **旧架构任务记录（`task_*`/`instant_task_*`）→ 永久垃圾** |
| `db/rc_task/` | 45 | 0.49 MB | 4.6 天 ~ 今天 | 现用任务记录（`rc_*`） |
| `db/upload/` | 90 | 0.05 MB | 18.1 天 ~ 今天 | 上传记录（新旧混） |
| `evidence/` | 144 | 5.20 MB | 4.6 ~ 3.2 天 | **YOLO 时代截图证据 → 永久垃圾** |
| `deploy/` | 1 | 7.54 MB | 4.4 天 | `a01-vlm-20260908.zip`（9/8 的 VLM 部署包备份，可留作回滚料） |
| `compressed/` | 0 | 0 | — | 空（生命周期已清干净） |

## 怎么算这笔账（重要）

- **存储费很小**：1.35 GB × 0.118 元/GB/月 ≈ **0.16 元/月**。清历史省的就是这个量级。
- **真正的钱是外网下行流量**：0.5 元/GB，而每场分析 VLM 要从源站拉 5 次视频。清历史**对它零帮助**。
- **永久垃圾只有 212 MB**（`outputs/` 206.31 + `evidence/` 5.20 + `db/task/` 0.59）。
- **视频本来就会自己消失**：`inputs/` 最老的只有 1.9 天，说明生命周期规则在正常工作。

## 三个可选范围

| 范围 | 内容 | 释放 | 风险 |
|---|---|---|---|
| A · 只清永久垃圾 | `outputs/` + `db/task/` + `evidence/` | 212 MB | 极低（对应功能都已删除） |
| B · 清垃圾 + 所有记录 | A + `db/upload/` + `db/rc_task/` | 212.5 MB | App 历史点开会 404；**Codex 若正在调试任务会被打断** |
| C · 全清 | B + `inputs/`（所有视频） | **1383.5 MB** | 同上 + 所有回放失效（本来就 1 天过期） |

`deploy/a01-vlm-20260908.zip` 建议**保留**（7.54 MB，是唯一可能可用的回滚料），或先下载到本地再删。

## 执行方式

维护版 `haoqiu-init`（`maintenance/haoqiu-init/`）：

```
{"action":"inventory"}                                  只读清单
{"action":"inventory","includeKeys":true}               清单 + 全部对象键
{"action":"purge","confirm":"PURGE-HISTORY","scope":"junk"}   清永久垃圾（范围 A）
{"action":"purge","confirm":"PURGE-HISTORY","scope":"all"}    全清（范围 C）
```

二次删除与精确范围（如"只删 `inputs/` 与 `db/task/`"）目前需要改 `JUNK_PREFIXES`/`SCAN_PREFIXES` 后重新部署。

## 收尾

清完后必须把 `haoqiu-init` 的**原版代码部署回去**（原版功能：建 `haoqiu_uploads`/`haoqiu_detection_tasks` 集合 + 配桶 CORS，由 `event.confirm === "haoqiu-init-v1"` 保护）。原版源码完整记录在 2026-09-12 的 `getFunctionDetail(haoqiu-init)` 返回里。
